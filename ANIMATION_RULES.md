# ANIMATION_RULES

静的サイトで使用するローディング、スクロール演出、フェードイン、メニュー開閉などのモーションルールを定義します。

## 基本方針

- アニメーションは情報理解、状態変化、視線誘導を助けるために使う
- 装飾のためだけに過度な動きを入れない
- 操作を待たせる演出は短くし、ユーザーの目的達成を妨げない
- `prefers-reduced-motion` に対応する
- JSが無効でも主要コンテンツを読める状態にする

## Loading

- ローディング画面は必要な場合のみ実装する
- 実際の読み込み状態と関係のない長いローディング演出を避ける
- ローディング完了後はフォーカスやスクロール位置を不自然に奪わない
- テキストやロゴだけが長時間表示される状態にしない
- 初回表示に必要なコンテンツを隠しすぎない

## Scroll reveal

- スクロール演出は表示の補助に限定し、コンテンツを読めなくしない
- 初期状態で `opacity: 0` のまま JS 失敗時に消え続けないようにする
- 画面下からのフェードインは距離と時間を控えめにする
- 重要なCTAやフォームを過度に遅延表示しない

## Menu and UI motion

- ハンバーガーメニュー、アコーディオン、タブなどは状態変化がわかる動きにする
- 開閉状態と `aria-expanded`、`hidden`、`data-state` を同期する
- メニュー開閉時は背景スクロール、フォーカス移動、Escapeキー対応を検討する
- アニメーション中でもクリック領域やフォーカス順が破綻しないようにする

## CSS

- 基本は CSS transition / animation を優先する
- `transform` と `opacity` を中心に使い、レイアウトを頻繁に再計算するプロパティを避ける
- アニメーション時間、イージング、遅延は CSS カスタムプロパティで管理する
- 無限ループのアニメーションは控えめにする

## JavaScript

- Intersection Observer はスクロール監視の第一候補にする
- スクロールイベントを使う場合は負荷に注意する
- 状態を class だけでなく、必要に応じて `data-state` や aria 属性にも反映する
- JSでアニメーションを制御する場合も、CSS側に静的なフォールバックを用意する

## Reduced motion

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    scroll-behavior: auto;
    animation-duration: 0.01ms;
    animation-iteration-count: 1;
    transition-duration: 0.01ms;
  }
}
```

## 納品前確認

- `prefers-reduced-motion` に対応している
- JS失敗時にも主要コンテンツが読める
- ローディング演出が不必要に長くない
- CTAやフォームが過度に遅延表示されない
- 開閉UIの aria 属性が状態と同期している
- スクロール時に横揺れや意図しない横スクロールが起きない
