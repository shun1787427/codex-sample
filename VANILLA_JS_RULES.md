# VANILLA_JS_RULES.md

## 基本方針

JavaScriptは必要最小限にする。
HTMLとCSSで解決できるものにJavaScriptを使わない。

## JavaScriptを使ってよいケース

- メニュー開閉
- アコーディオン
- タブ
- モーダル
- フォームバリデーション補助
- スクロールに応じた軽い演出
- 入力内容に応じた表示切り替え

## JavaScriptを使わない方がよいケース

- CSSだけで可能なホバー表現
- CSSだけで可能なアニメーション
- HTMLの標準機能で足りるフォーム
- 単純な表示装飾

## 実装ルール

- DOM取得は必要な範囲に限定する
- data属性を活用する
- クラスの付け外しで状態を表現する
- JS内にスタイル値を直接書きすぎない
- イベントリスナーは重複登録しない
- nullチェックを行う
- DOMContentLoaded 以降に初期化する
- 機能ごとに関数を分ける

## 状態管理

静的サイトでは複雑な状態管理を行わない。
状態は基本的に以下で扱う。

- aria-expanded
- hidden
- data-state
- class
- localStorage

## アクセシビリティ

メニュー、アコーディオン、タブ、モーダルを実装する場合は、キーボード操作とaria属性を確認する。