# REVIEW_CHECKLIST.md

## 基本方針

実装後は、見た目だけでなく、要件、HTML構造、CSS設計、アクセシビリティ、レスポンシブ、JavaScript、保守性の観点から確認する。

このプロジェクトでは、原則として HTML / CSS / Vanilla JavaScript による静的サイト制作を前提とする。

React / Vue / Next.js / Nuxt / Svelte / Tailwind CSS / Bootstrap などは、ユーザーが明示的に許可した場合、または要件上の必要性が明確な場合を除き使用しない。

## 1. 要件確認

- ユーザーの依頼内容を満たしているか
- サイトの目的が明確か
- 想定ユーザーに合った構成になっているか
- 主要CTAが明確か
- 必要なページまたはセクションが揃っているか
- ファーストビューで提供価値が伝わるか
- 信頼要素が不足していないか
- 問い合わせ・購入・予約などの導線が自然か
- 不足情報を仮定した場合、その内容を明記しているか

## 2. 技術選定

- 原則どおり HTML / CSS / Vanilla JavaScript で実装されているか
- 不要なフレームワークを導入していないか
- CSSフレームワークを使用していないか
- npm依存を不要に増やしていないか
- SSGを使う場合、その理由が明確か
- Astroなどを提案する場合、素のHTMLとの比較ができているか

## 3. HTML / マークアップ

- セマンティックHTMLになっているか
- `header`, `nav`, `main`, `section`, `article`, `aside`, `footer` が適切に使われているか
- h1 は原則1つか
- 見出し階層が自然か
- section に必要な見出しがあるか
- リスト構造に `ul`, `ol`, `dl` が使われているか
- ページ遷移に `a` が使われているか
- UI操作に `button` が使われているか
- div / span の乱用がないか
- 不要な aria が付与されていないか

## 4. CSS設計

- CSSフレームワークに依存していないか
- CSSカスタムプロパティを活用しているか
- 色・余白・フォントサイズ・角丸・影がトークン化されているか
- 場当たり的な値が増えていないか
- `!important` を乱用していないか
- インラインstyleを乱用していないか
- レイアウト、コンポーネント、ユーティリティの責務が混ざりすぎていないか
- クラス名が役割ベースで分かりやすいか
- 似たUIに似たCSSが使われているか
- 将来的に修正しやすい構造か

## 5. デザイン・UI

- サイト全体のトーンが統一されているか
- 余白に一貫性があるか
- 見出し・本文・補足テキストの階層が分かりやすいか
- CTAが目立つか
- 重要情報が埋もれていないか
- 装飾が過剰ではないか
- 信頼感・清潔感・訴求力など、依頼された雰囲気に合っているか
- 同じ役割のUIが同じ見た目になっているか

## 6. レスポンシブ

- モバイルファーストで設計されているか
- 320px幅でも内容が読めるか
- 375px / 390px / 428px で破綻しないか
- 768px幅でレイアウトが自然か
- 1024px以上でPC表示として自然か
- 横スクロールが発生していないか
- 画像が親要素からはみ出していないか
- grid / flex の子要素に `min-width: 0` が必要ではないか
- テキスト行長が長すぎないか
- CTAがスマートフォンでも押しやすいか
- フォームがスマートフォンで入力しやすいか

## 7. アクセシビリティ

- 見出し階層が自然か
- 画像に適切な alt があるか
- 装飾画像は `alt=""` になっているか
- リンクテキストだけで遷移先が分かるか
- button と a の役割が混同されていないか
- キーボード操作が可能か
- focus-visible が確認できるか
- 色だけで情報を伝えていないか
- テキストと背景のコントラストが不足していないか
- フォームに label があるか
- エラー文が入力欄と関連しているか
- aria を過剰に使っていないか

## 8. Vanilla JavaScript

- JavaScriptは必要最小限か
- CSSだけでできる表現にJSを使っていないか
- DOM取得に null チェックがあるか
- イベントリスナーが重複していないか
- data属性を適切に使っているか
- JS内にスタイル値を直接書きすぎていないか
- class と aria 状態が同期しているか
- メニュー開閉で `aria-expanded` が更新されるか
- アコーディオンやタブの状態がHTML属性に反映されているか
- JavaScript無効時にも最低限の情報が読めるか

## 9. UI状態

- 状態管理のためにフレームワークを導入していないか
- UI状態は対象コンポーネント内で完結しているか
- `is-active`, `is-open`, `data-state` などの命名が分かりやすいか
- `hidden`, `aria-expanded`, `aria-selected`, `aria-current` が適切に使われているか
- localStorage を不要に使っていないか
- 初期状態がHTML上で分かるか

## 10. パフォーマンス

- 不要なCSSやJSが多すぎないか
- 画像サイズが過剰ではないか
- 画像に width / height が指定されているか
- lazy loading を検討しているか
- フォント読み込みが重すぎないか
- 外部スクリプトを不要に増やしていないか
- アニメーションが過剰ではないか
- レイアウトシフトを起こしそうな要素がないか

## 11. SEO基本

- title がページ内容に合っているか
- meta description が設定されているか
- h1 がページの主題を表しているか
- 見出しに検索意図と内容が反映されているか
- 画像altが適切か
- 内部リンク導線が自然か
- OGP設定が必要に応じて用意されているか
- canonical が必要な場合に設定されているか
- robots設定に問題がないか

## 12. ファイル構成

- 小規模サイトとして分かりやすい構成か
- assets配下にCSS / JS / imagesが整理されているか
- CSSファイルが肥大化しすぎていないか
- JSファイルが機能ごとに読みやすいか
- 画像ファイル名が分かりやすいか
- 不要ファイルが残っていないか

## 13. 保守性

- 他人が読んでも意図が分かるコードか
- クラス名・関数名が自然か
- 同じようなコードが重複していないか
- 変更に弱い構造になっていないか
- コメントが必要な箇所にだけ書かれているか
- 将来的にセクション追加しやすいか
- デザイン変更時に修正箇所が分散しすぎないか

## 14. 納品前確認

- 主要ブラウザで表示確認したか
- スマートフォン表示を確認したか
- リンク切れがないか
- フォームの送信先や仕様が明確か
- 画像パスが正しいか
- CSS / JS の読み込みパスが正しいか
- コンソールエラーがないか
- 不要なconsole.logが残っていないか
- 仮テキストが残っていないか
- 仮画像が残っていないか

## 15. レビュー出力形式

レビュー時は以下の形式でまとめる。

### 良い点

- 

### 修正が必要な点

| 優先度 | 箇所 | 内容 | 対応案 |
| --- | --- | --- | --- |
| 高 |  |  |  |
| 中 |  |  |  |
| 低 |  |  |  |

### 追加で確認したい点

- 

### 最終判断

- 公開可能
- 軽微な修正後に公開可能
- 修正後に再確認が必要