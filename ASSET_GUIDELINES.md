# ASSET_GUIDELINES

静的サイトで使用する画像、アイコン、フォント、動画、外部素材の扱いを定義します。

## 基本方針

- 素材は目的、意味、導線への貢献が明確なものだけを使用する
- 装飾目的だけの重い画像や動画は避ける
- 実在の商品、場所、人物、実績を伝えるページでは、内容を確認できる画像を優先する
- 仮画像、仮ロゴ、仮アイコンを残したまま納品しない
- 著作権、ライセンス、利用範囲が不明な素材は使用しない

## 画像

- 主要画像には `width` と `height` を指定し、レイアウトシフトを防ぐ
- ファーストビュー画像は画質と読み込み速度のバランスを確認する
- 下部セクションの画像には原則 `loading="lazy"` を指定する
- `srcset` / `sizes` は必要に応じて使用し、過剰に複雑化しない
- 写真は WebP または JPEG、透過が必要な画像は PNG または WebP を検討する
- 画像内に重要な文字情報を閉じ込めない
- 画像ファイル名は内容がわかる英数字とハイフンで命名する

## alt

- 意味のある画像には、周辺文脈と重複しすぎない `alt` を設定する
- 装飾画像は `alt=""` とし、スクリーンリーダーに読ませない
- リンク画像はリンク先や操作内容がわかる `alt` にする
- 写真の雰囲気だけでなく、ユーザー判断に必要な対象を説明する
- `画像`, `写真`, `アイコン` だけの alt にしない

## アイコン

- 意味のあるアイコンボタンにはテキストラベル、`aria-label`、または視覚的に隠したラベルを用意する
- 装飾アイコンは `aria-hidden="true"` を指定する
- SVG は必要に応じてインライン化し、色やサイズを CSS で管理できる形にする
- アイコンだけで状態を伝えず、色以外の手がかりも用意する

## フォント

- 外部 Web フォントは必要性、表示速度、ライセンスを確認してから使用する
- 本文はシステムフォントでも成立する設計を優先する
- `font-display: swap` を検討する
- 日本語フォントは容量が大きくなりやすいため、読み込み範囲と代替フォントを確認する

## 動画

- 自動再生する場合は原則 `muted` とし、ユーザーの操作を妨げない
- 背景動画は内容理解に必須の情報を持たせない
- `poster` を設定し、読み込み前の表示崩れを防ぐ
- 長尺動画は外部プレイヤーや遷移先を検討する
- 音声が重要な動画には字幕や代替テキストを検討する

## 納品前確認

- 仮画像、仮ロゴ、透かし入り画像が残っていない
- 画像の容量が過大ではない
- 主要画像に `width` / `height` がある
- 意味のある画像に適切な `alt` がある
- 装飾画像が読み上げ対象になっていない
- 素材のライセンスと出典が確認済み
- 404 になる画像、フォント、動画がない
