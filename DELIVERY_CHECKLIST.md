# DELIVERY_CHECKLIST

静的サイトの納品前に確認する実務項目を定義します。

## Scope

- 依頼されたページ、セクション、導線がそろっている
- 未対応項目がある場合は理由と対応案を明記する
- 仮定した内容が作業結果にまとまっている

## Files

- 不要なテストファイル、バックアップファイル、空ファイルがない
- ファイル名とディレクトリ名が英数字、ハイフン、必要に応じたアンダースコアで統一されている
- HTML、CSS、JS、画像の参照パスが正しい
- 別案件のファイルや文言が混ざっていない
- `.gitignore` が必要な生成物やローカルファイルを除外している

## HTML

- `lang`、`meta charset`、`viewport` が正しい
- 見出し階層が破綻していない
- リンク切れ、空リンク、`href="#"` の放置がない
- ボタンとリンクの役割が適切に分かれている
- 外部リンクの扱いが自然である

## CSS

- デザイントークンが活用されている
- 不要な重複、未使用の大きなスタイル、デバッグ用スタイルが残っていない
- 横スクロールの原因になる固定幅がない
- モバイル、タブレット、デスクトップで表示が破綻しない

## JavaScript

- 不要な JS 依存がない
- `console.log`、`debugger`、仮の処理が残っていない
- UI状態と aria 属性が同期している
- JSエラーが発生していない
- JSが無効でも主要情報が読める

## Assets

- 仮画像、透かし画像、別案件画像が残っていない
- 画像容量が過大ではない
- alt が適切である
- favicon、OGP画像、ロゴの表示を確認している

## SEO and sharing

- `title`、`description`、OGP、canonical を確認している
- 本番URL未確定の項目は作業結果に明記している
- `noindex` やテストURLが残っていない

## Accessibility

- キーボード操作で主要導線を利用できる
- focus-visible が見える
- 色だけに依存した情報伝達がない
- フォームの label、エラー、必須表示が適切である

## Final output

- 実装内容
- 作成・変更ファイル
- 仮定した内容
- 公開前レビュー
- レスポンシブ確認
- アクセシビリティ確認
- 最終判断
