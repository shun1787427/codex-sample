# FORM_RULES

静的サイトにおける問い合わせ、予約、資料請求、申し込みフォームの設計と実装ルールを定義します。

## 基本方針

- フォームは入力完了だけでなく、ユーザーが迷わず送信できることを重視する
- 静的サイトでは送信先、メールフォーム、外部サービス、バックエンド有無を最初に確認する
- 送信処理が未実装の場合、送信できるように見せかけない
- バリデーションは補助であり、HTMLの標準機能を優先する

## HTML

- 入力項目には必ず `label` を関連付ける
- `placeholder` を `label` の代替にしない
- 入力種別に合わせて `type="email"`、`type="tel"`、`type="url"` などを使う
- 必須項目には `required` を指定し、視覚的にも必須であることを示す
- 関連項目は `fieldset` と `legend` を検討する
- 送信ボタンは `button type="submit"` を使用する

## Error and help text

- 入力補助文は対象フィールドの近くに置く
- エラー文は何が問題で、どう直せばよいかを示す
- JSでエラー表示を行う場合は `aria-invalid` と `aria-describedby` を同期する
- エラー表示だけを色に依存させない

## Static site handling

- 送信先が決まっていない場合は、フォームではなく `mailto:`、電話リンク、外部予約リンクなどを検討する
- `mailto:` を使う場合は、ユーザーのメールアプリが開くことを前提に文脈を整理する
- 外部フォームに遷移する場合は、リンクであることが自然に伝わる文言にする
- 実際の送信完了画面がない場合、完了メッセージを擬似的に表示しない

## JavaScript

- JSは入力補助、表示切り替え、軽いバリデーションに限定する
- HTML標準のバリデーションで足りる場合はJSを追加しない
- 送信を止める処理を入れる場合は、ユーザーに理由を表示する
- `console.log` を残さない

## Accessibility

- キーボードだけで全項目に到達し、送信できるようにする
- フォーカス位置がわかるようにする
- エラー発生時はエラー概要または最初のエラー項目へ誘導する
- 必須表示はスクリーンリーダーにも伝わるようにする

## 納品前確認

- `label` と入力欄が関連付いている
- 必須、任意、入力例がわかる
- 送信先または代替導線が明確である
- 送信できないフォームを送信可能に見せていない
- エラー表示が色だけに依存していない
- キーボード操作で完結できる
- `console.log`、仮のエンドポイント、テストメールアドレスが残っていない
